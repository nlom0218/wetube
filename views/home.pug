extends layouts/main
include mixins/videoBlock

block content
    h3 Hello everyone!
    .videos
        each item in videos
            +videoBlock({
                videoFile:item.fileUrl,
                id:item.id,
                title:item.title,
                views:item.views
            })